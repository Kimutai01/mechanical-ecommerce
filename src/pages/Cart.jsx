import React from "react";

import { useSelector } from "react-redux";

const Cart = () => {
  //   const cart = useSelector(selectCart);
  //   console.log(cart);
  return <div className="pt-[400px]"></div>;
};

export default Cart;
